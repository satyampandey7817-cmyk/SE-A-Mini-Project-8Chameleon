name: student_presence_system
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  web: ^1.1.0
  js: ^0.6.7
  flutter:
    sdk: flutter
  excel: ^4.0.3

  cupertino_icons: ^1.0.8

  # ✅ Firebase
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  firebase_messaging: ^15.1.3
  cloud_functions: ^5.1.3
  firebase_storage: ^12.3.2   # ← MOVED HERE inside dependencies

  # ✅ Notifications
  flutter_local_notifications: ^17.2.3

  # ✅ Utilities
  url_launcher: ^6.3.2
  intl: ^0.19.0
  file_picker: ^10.3.10
  csv: ^7.2.0
  share_plus: ^12.0.2
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true